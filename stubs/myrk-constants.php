<?php
/**
 * Plugin-level constants — for static analysis only, never loaded at runtime.
 * These are defined in myrk-feature-flags.php before the autoloader runs.
 */
define( 'MYRK_VERSION', '1.0.0' );
define( 'MYRK_FILE', '' );
define( 'MYRK_DIR', '' );
define( 'MYRK_URL', '' );
