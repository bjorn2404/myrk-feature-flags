{
    "name": "myrk",
    "packageManager": "pnpm@11.6.0",
    "scripts": {
        "build": "wp-scripts build",
        "start": "wp-scripts start",
        "lint:js": "wp-scripts lint-js",
        "lint:css": "wp-scripts lint-style",
        "test": "wp-scripts test-unit-js",
        "package": "pnpm build && rm -rf .release myrk-feature-flags.zip && mkdir -p .release/myrk-feature-flags && rsync -a --exclude-from=.distignore . .release/myrk-feature-flags/ && (cd .release && zip -r ../myrk-feature-flags.zip myrk-feature-flags) && rm -rf .release && echo 'Built: myrk-feature-flags.zip'"
    },
    "devDependencies": {
        "@wordpress/icons": "^14.0.0",
        "@wordpress/scripts": "^30.0.0"
    },
    "dependencies": {
        "@wordpress/dataviews": "^4.9.1"
    }
}
