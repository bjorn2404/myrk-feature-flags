{
    "name": "myrk",
    "packageManager": "pnpm@11.6.0",
    "scripts": {
        "build": "wp-scripts build",
        "start": "wp-scripts start",
        "lint:js": "wp-scripts lint-js",
        "lint:css": "wp-scripts lint-style",
        "test": "wp-scripts test-unit-js",
        "package": "pnpm build && rm -rf .release && mkdir -p .release/myrk && rsync -a --exclude-from=.distignore . .release/myrk/ && (cd .release && zip -r ../myrk.zip myrk) && rm -rf .release && echo 'Built: myrk.zip'"
    },
    "devDependencies": {
        "@wordpress/icons": "^14.0.0",
        "@wordpress/scripts": "^30.0.0"
    },
    "dependencies": {
        "@wordpress/dataviews": "^4.9.1"
    }
}
